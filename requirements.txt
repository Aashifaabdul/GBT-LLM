# Pinned to what is confirmed working (2026-08-07) on the dev machine:
# RTX 5050 Laptop (8GB VRAM, Blackwell-class / sm_120), Python 3.11, Windows 11.
#
# torch/torchvision: already installed with CUDA 13.0 wheels. If reinstalling
# on a fresh machine:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
torch==2.11.0
torchvision==0.26.0

transformers==5.14.1
# accelerate must be new enough for peft>=0.20 (0.24.1 is too old -- breaks
# `from accelerate.utils.memory import clear_device_cache`, confirmed 2026-08-07)
accelerate>=1.0
peft>=0.13,<1.0

numpy==2.4.2
pillow==12.3.0
scipy==1.17.1
scikit-learn==1.8.0
scikit-image>=0.24
matplotlib==3.10.8
tqdm==4.67.3

gradio==6.15.2
pytest>=8.0
